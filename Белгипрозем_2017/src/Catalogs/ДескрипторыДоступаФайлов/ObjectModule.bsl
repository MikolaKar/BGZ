
Процедура ПриЗаписи(Отказ)
	
	// Если это очистка устаревших дескрипторов, пересчет прав не нужен
	Если ПометкаУдаления Тогда
		Возврат;
	КонецЕсли;
	
	Справочники.ДескрипторыДоступаФайлов.ОбновитьПрава(
		ЭтотОбъект.Ссылка,
		Неопределено, // Протокол
		Истина);      // Немедленно
	
КонецПроцедуры
