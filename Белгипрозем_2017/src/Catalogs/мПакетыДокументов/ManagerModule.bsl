Функция ПолучитьДокументыИзПакета(ПакетДокументов) Экспорт

	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	ПакетыДокументов.Ссылка КАК ДокументЗаказчика
		|ИЗ
		|	Справочник.ПакетыДокументов КАК ПакетыДокументов
		|ГДЕ
		|	ПакетыДокументов.Родитель = &ПакетДокументов
		|	И НЕ ПакетыДокументов.ПометкаУдаления";

	Запрос.УстановитьПараметр("ПакетДокументов", ПакетДокументов);

	Результат = Запрос.Выполнить().Выгрузить();

	СписокДокументов = Новый СписокЗначений;
	
	СписокДокументов.ЗагрузитьЗначения(Результат.ВыгрузитьКолонку("ДокументЗаказчика"));
	
	Возврат СписокДокументов;
	
КонецФункции // ПолучитьДокументыИзПакета(ПакетДокументов)
