&НаСервере
Процедура ЗаписатьПрейскурант(Ссылка, Таблица) Экспорт

	НаборЗаписей = РегистрыСведений.мПрейскурант.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.Регистратор.Установить(Ссылка);
	НаборЗаписей.Загрузить(Таблица);
	НаборЗаписей.Записать(Истина);

КонецПроцедуры // ЗаписатьПрейскурант()
 