
// Хранение контекста взаимодействия с сервисом
&НаКлиенте
Перем КонтекстВзаимодействия Экспорт;

#Область ОбработчикиСобытийФормы

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	ИнтернетПоддержкаПользователейКлиент.ОбработатьОткрытиеФормы(КонтекстВзаимодействия, ЭтотОбъект);
	
	ОсновныеПараметры = КонтекстВзаимодействия.КСКонтекст.ОсновныеПараметры;
	
	Если ТипЗнч(ОсновныеПараметры) = Тип("Структура")
		И ОсновныеПараметры.Свойство("МестоЗапуска") Тогда
		
		Если ОсновныеПараметры.МестоЗапуска = "handStartNew"
			ИЛИ ОсновныеПараметры.МестоЗапуска = "systemStartNew" Тогда
			Элементы.Декорация1.Заголовок = НСтр("ru = 'Показ информационного окна недоступен для этой конфигурации.'");
			
		ИначеЕсли ОсновныеПараметры.МестоЗапуска = "taxcomGetID" Тогда
			Элементы.Декорация1.Заголовок = НСтр("ru = 'Получение уникального идентификатора абонента Такском недоступно для этой конфигурации.'");
			
		ИначеЕсли ОсновныеПараметры.МестоЗапуска = "taxcomPrivat" Тогда
			Элементы.Декорация1.Заголовок = НСтр("ru = 'Работа с личным кабинетом абонента недоступна для этой конфигурации.'");
			
		Иначе
			Элементы.Декорация1.Заголовок = НСтр("ru = 'Выбранное действие недоступно для этой конфигурации.'");
			
		КонецЕсли;
		
	Иначе
		
		Элементы.Декорация1.Заголовок = НСтр("ru = 'Выбранное действие недоступно для этой конфигурации.'");
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура КомандаОК(Команда)
	
	Закрыть();
	
КонецПроцедуры

#КонецОбласти